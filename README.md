# Grid Dominion

Grid Dominion is a full-stack grid-based multiplayer strategy game (MERN + WebSockets). This repository contains the client built with Vite + React + TypeScript and a Node/Express server for matchmaking and game sockets.

## Structure

- `src/` — Frontend (React + TypeScript)
- `server/` — Backend (Node/Express, socket handling, controllers, models)
- `public/` — Static assets

## Quick start

1. Install dependencies:

```bash
npm install
cd server
npm install
```

2. Run development servers (two terminals):

```bash
# terminal 1 (frontend)
npm run dev

# terminal 2 (backend)
cd server
npm start
```

Adjust commands as needed depending on your environment and package scripts.

## Notes

- This repository was pushed as a fresh repo to remove prior commit history.
- Keep a backup of the original repository if you need the old history.


Made by Ritesh
